public class Abtin
{
    // Sample data to test your program
    public static void Main() {}

    // Implement the following functions

    // 1. GetPeople
    // This function should return an array of all the people mentioned in the given string.
    // A person's name based on the format we've been given is a word that starts with 
    // an uppercase letter and is followed by lowercase letters.
    public string[] GetPeople(string sentence)
    {
        // Implement your solution here using the `sentence` variable above
        return new string[] { }; // You will need to change this line to return the actual result
    }

    // 2. GetPlaces
    // This function should return an array of all the places mentioned in the given string.
    // A place based on the format we've been given is a word that is in all uppercase letters.
    public string[] GetPlaces(string sentence)
    {
        // Implement your solution here using the `sentence` variable above
        return new string[] { }; // You will need to change this line to return the actual result
    }

    // 3. RetrieveData
    // This function should return the data split into sentences.
    // A sentence ends with a period (.) a question mark (?) or an exclamation mark (!).
    public string[] RetrieveData(string data)
    {
        // Implement your solution here using `data` variable
        return new string[] {};
    }
}
